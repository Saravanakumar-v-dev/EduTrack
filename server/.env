# ====================================================================
# APPLICATION ENVIRONMENT CONFIGURATION
# This file contains sensitive information and should NOT be committed to git.
# ====================================================================

# 1. SERVER CONFIGURATION
PORT=5000
NODE_ENV=development # Use 'production' for deployment

# 2. MONGODB CONNECTION
# Mongoose will use this URI to connect to your database.
# Using the local MongoDB instance on the default port:
MONGO_URI="mongodb://localhost:27017/edutrack_mern"


# 3. JSON WEB TOKEN (JWT) SECRET
# This must be a long, complex, and random string. It is used to sign/verify tokens.
JWT_SECRET="your_very_secure_and_long_random_jwt_secret_key_1234567890"


# 4. EMAIL SERVICE (NODEMAILER) CONFIGURATION
# These are required by server/services/emailService.js
# NOTE: For Gmail, you MUST use an App Password, not your regular password.
# To test locally without a real SMTP, use Ethereal (https://ethereal.email/)
# .env file

# 4. EMAIL SERVICE (NODEMAILER) CONFIGURATION
# These values are temporary and come directly from Ethereal.email
EMAIL_HOST="smtp.ethereal.email" 
EMAIL_PORT=587               
EMAIL_USER="chaz.walter@ethereal.email" # <--- This is your Ethereal Username
EMAIL_PASS="a4d8MAp3XT32HfAKc8"         # <--- This is your Ethereal Password

# 5. CLIENT URL
# Required for constructing the password reset link in the emailService.
# Use your frontend's actual URL (e.g., https://app.yoursite.com in production)
CLIENT_URL="http://localhost:3000"


# 6. OTHER SECRETS (Optional, but good practice for future features)
# API_KEY_EXTERNAL_SERVICE="some_external_key"